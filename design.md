Create a single Python file utilizing FastAPI for the web server and SQLite for the database, chosen for their zero-configuration deployment and minimal resource footprint, allowing the entire application to run on the most basic Linux VM. The architecture deliberately avoids heavy LLM frameworks and vector databases to eliminate dependency churn; instead, it uses Python's standard urllib library to make direct REST calls to OpenAI or Anthropic, keeping the codebase stable and transparent. Institutional knowledge is handled by embedding static information directly into the system prompt for reliability, while complex queries rely on the model's inference capabilities, removing the need for a custom search tool. The front end consists of a lightweight, embeddable JavaScript widget for students and a basic HTML admin panel protected by Google OAuth, ensuring that the entire system remains a cohesive, maintainable unit with fewer than five core dependencies.
